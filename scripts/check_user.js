const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGODB_URI).then(async () => { const db = mongoose.connection.db; const settings = await db.collection('usersettings').findOne({ userId: '69363accf27347a36c5c05c2' }); console.log('Telegram Settings:', JSON.stringify(settings?.telegram, null, 2)); const auto = await db.collection('automationconfigs').findOne({ userId: '69363accf27347a36c5c05c2' }); console.log('Automation:', JSON.stringify(auto, null, 2)); process.exit(0); });